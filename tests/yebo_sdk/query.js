// Testing requests
import req from '../../lib/yebo_sdk/query';

//
var expect = require('chai').expect,
    should = require('chai').should();

module.exports = function() {
  describe('Query', () => {
    // IT template
    // it('', (done) => {
    //
    // });

    //
    it('should appear', (done) => {
      done();
    });
  });
};
