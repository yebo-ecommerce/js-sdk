//
import chai from 'chai';

//
var Cart = YeboSDK.Cart,
    expect = chai.expect,
    should = chai.should();

module.exports = function() {
  describe('Cart', () => {
    // IT template
    // it('', (done) => {
    //
    // });
  });
};
