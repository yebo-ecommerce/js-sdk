/**
 * Method responsible for seeking taxons
 * @param {String} ids Category base
 * @param {Array of objects} ranges Price Range
 * @param {Object} search Parameters used in the products search
 * @return
 * @example
 *   getTaxons('1,2,3,4', 'corredores,frutas')
 */
export const getTaxons = function (ids, taxons, taxonomyId, page = 1, perPage = 25) {
  // ...
}
